{
  "name": "skeb-explorer",
  "version": "1.0.0",
  "description": "An artist explorer for Skeb in case for future commissions",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "node app.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix frontend && npm run build --prefix frontend"
  },
  "author": "apmariano",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.24.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  }
}
